# План реализации лендинг пейджа для tscodex.com

## Общая концепция

**TSCodex** — проект для создания инструментов работы с LLM (Large Language Models). Первый продукт — **MCP Manager** — десктопное приложение на Electron для управления MCP (Model Context Protocol) серверами.

### Ключевая идея

MCP Manager решает проблему работы с MCP серверами в реальных проектах:
- Каждый проект в Cursor требует свой workspace
- Один сервер должен работать с разными рабочими областями
- Необходима инкапсуляция и безопасность
- Нужен визуальный интерфейс для управления

---

## Цветовая схема

### Основные цвета

- **Background (основной)**: `#0a0a0f` — очень темный фон
- **Surface (карточки)**: `#12121a` — темные поверхности
- **Border**: `#1e1e2e` — границы элементов
- **Primary (бирюзовый)**: `#00d4aa` → `#00b894` (градиент)
- **Secondary (синий)**: `#0ea5e9` — акцентный синий
- **Text Primary**: `#ffffff` — основной текст
- **Text Secondary**: `#9ca3af` — вторичный текст

### Градиенты

- **Accent Gradient**: `linear-gradient(135deg, #00d4aa, #0ea5e9)` — бирюзово-синий градиент
- **Background Gradient**: `linear-gradient(180deg, #0a0a0f 0%, #0f1419 100%)` — темный градиент фона

### Стиль

- **Тема**: AI-стиль с бирюзово-зелеными оттенками
- **Тон**: Темные тона с легким бирюзовым оттенком в градиентах
- **Эстетика**: Современный, технологичный, минималистичный

---

## Структура страницы

### 1. Header (Шапка)

#### Элементы:
- **Логотип TSCodex** (слева)
- **Две кнопки** (справа):
  - **MCP Manager** → `https://github.com/unbywyd/tscodex-mcp-manager-app`
  - **Cursor Bridge** → `https://github.com/unbywyd/tscodex-mcp-manager`

#### Стиль:
- Фиксированная шапка при скролле
- Прозрачный фон с blur эффектом
- Кнопки с градиентным фоном при hover

---

### 2. Hero Section (Главная секция)

#### Заголовок:
```
TSCodex - LLM Development Tools
```

#### Подзаголовок:
```
Инструменты для работы с Large Language Models
```

#### Описание:
```
MCP Manager — standalone desktop приложение для управления MCP серверами.
Централизованный хаб для настройки, запуска и мониторинга MCP серверов
с гранулярным контролем безопасности и интеграцией AI.
```

#### CTA кнопки:
- **Download** (основная, с градиентом)
- **View on GitHub** (вторичная, outline стиль)

#### Визуализация:
- Фоновое изображение с градиентным overlay
- Иконка/логотип MCP Manager в центре или справа

---

### 3. Секция: Что такое MCP Manager

#### Заголовок:
```
Что такое MCP Manager?
```

#### Описание:
```
Standalone desktop приложение для управления MCP (Model Context Protocol) серверами.
MCP Manager предоставляет централизованный хаб для настройки, запуска и мониторинга
MCP серверов с гранулярным контролем безопасности и интеграцией AI.
```

#### Ключевые возможности (карточки):

1. **Управление процессами**
   - Запуск, остановка, перезапуск MCP серверов
   - Мониторинг состояния в реальном времени
   - Автоматическое восстановление при ошибках

2. **Визуальный интерфейс настройки**
   - Отображение tools, resources, prompts
   - JSON Schema конфигурация через UI
   - Интуитивное управление

3. **Секреты и авторизация**
   - 3 уровня переопределения: Global → Workspace → Server
   - Хранение в OS Keychain (безопасно)
   - Секреты никогда не в plain text

4. **AI агент интеграция**
   - OpenAI-совместимый API прокси
   - Статистика использованных токенов
   - Rate limiting и модель restrictions

5. **Динамическое создание инструментов**
   - MCP Tools — динамический сервер
   - Создание tools/resources без кода
   - AI-assisted генерация через промпт

---

### 4. Секция: Архитектура (React Flow визуализация)

#### Заголовок:
```
Архитектура решения
```

#### Описание:
```
Визуализация взаимодействия компонентов системы через React Flow
```

#### Диаграмма:

```
┌─────────────────┐
│     Cursor      │
│   (IDE Editor)  │
└────────┬────────┘
         │
         │ Bridge Extension
         │ (автоматическая регистрация)
         ▼
┌─────────────────┐     ┌─────────────────┐
│  Cursor Bridge  │────▶│   MCP Manager   │
│   Extension     │     │  (Desktop App)  │
└─────────────────┘     └────────┬────────┘
                                 │
                                 │ Workspace Proxying
                                 │ (один сервер → множество workspace)
                                 ▼
                    ┌─────────────────────────┐
                    │    MCP Servers          │
                    │  (HTTP MCP Servers)     │
                    └────────────┬────────────┘
                                 │
                                 │ SDK Integration
                                 ▼
                    ┌─────────────────────────┐
                    │   @tscodex/mcp-sdk      │
                    │   (Core SDK)            │
                    └─────────────────────────┘
```

#### Текстовая версия (для SEO):

**Компоненты системы:**

1. **Cursor Bridge Extension**
   - VS Code/Cursor расширение
   - Автоматическая регистрация workspace по пути проекта
   - Синхронизация с MCP Manager
   - Регистрация прокси MCP серверов в локальном `mcp.json`

2. **MCP Manager (Desktop App)**
   - Electron приложение
   - Управление процессами MCP серверов
   - Workspace proxying (один сервер → множество workspace)
   - HTTP API на localhost (127.0.0.1)

3. **MCP Servers**
   - HTTP MCP серверы на базе SDK
   - Пример: `@tscodex/mcp-images`
   - Получают заголовки от текущего workspace
   - Работают с PROJECT_ROOT из workspace

4. **@tscodex/mcp-sdk**
   - Core SDK для создания MCP серверов
   - Быстрое создание HTTP MCP серверов
   - Поддержка workspace context

---

### 5. Секция: Workspaces & Proxying

#### Заголовок:
```
Workspaces & Proxying
```

#### Проблема:
```
Каждый проект в Cursor требует свой workspace.
MCP серверы должны работать с конкретным проектом,
но один сервер не может знать о разных проектах одновременно.
```

#### Решение:
```
MCP Manager создает workspace прокси для каждого проекта.
Один сервер → множество workspace прокси.
SDK получает заголовки от текущего workspace и позволяет
одному серверу использовать с разными рабочими областями.
```

#### Как это работает:

1. **Автоматическая регистрация**
   - Cursor Bridge автоматически регистрирует workspace в MCP Manager
   - Путь проекта передается автоматически

2. **Workspace Proxying**
   - MCP Manager создает прокси для каждого workspace
   - Каждый прокси передает свой PROJECT_ROOT в сервер
   - Сервер получает контекст текущего workspace

3. **Синхронизация**
   - Cursor Bridge синхронизирует курсор с MCP Manager
   - Автоматическая регистрация прокси MCP серверов в `mcp.json`
   - Идеальная инкапсуляция и связь между workspace

#### Визуализация:

```
Project A (/path/to/project-a)
    └─▶ Workspace Proxy A ──▶ MCP Server Instance A
                                    │
                                    ▼
                            PROJECT_ROOT=/path/to/project-a

Project B (/path/to/project-b)
    └─▶ Workspace Proxy B ──▶ MCP Server Instance B
                                    │
                                    ▼
                            PROJECT_ROOT=/path/to/project-b
```

---

### 6. Секция: Features (детальные карточки)

#### 6.1 Server Management

**Заголовок:** `Управление серверами`

**Описание:**
```
Добавление серверов через различные менеджеры пакетов:
npm, npx, pnpx, yarn, bunx, local path
```

**Возможности:**
- ✅ Визуальное отображение tools, resources, prompts
- ✅ JSON Schema конфигурация через UI
- ✅ Запуск/остановка/перезапуск серверов
- ✅ Мониторинг состояния в реальном времени
- ✅ Логи и отладка

**Ссылки:**
- Пример сервера: [`@tscodex/mcp-images`](https://github.com/unbywyd/tscodex-mcp-images)
- SDK: [`@tscodex/mcp-sdk`](https://www.npmjs.com/package/@tscodex/mcp-sdk)

---

#### 6.2 Dynamic MCP Tools

**Заголовок:** `Динамические инструменты`

**Описание:**
```
Встроенный MCP Tools сервер позволяет создавать кастомные инструменты,
промпты и ресурсы без написания отдельного пакета.
```

**Типы инструментов:**

1. **Static**
   - Возвращает фиксированный контент
   - Использование: константы, шаблоны, документация

2. **HTTP**
   - Выполняет HTTP запросы
   - Использование: API интеграции, webhooks
   - Поддержка секретов через `{{SECRET_KEY}}`

3. **Function**
   - Выполняет JavaScript код
   - Использование: обработка данных, сложная логика
   - Доступ к context (workspace, project root, etc.)

**AI-Assisted генерация:**
- ✨ Кнопка "AI" при создании инструментов
- Генерация через промпт на естественном языке
- Автоматическое заполнение форм
- Требует настройки AI Agent

**Ссылки:**
- Документация: [MCP Tools Guide](https://github.com/unbywyd/tscodex-mcp-manager-app#dynamic-tools-mcp-tools)

---

#### 6.3 Secrets Management

**Заголовок:** `Управление секретами`

**Описание:**
```
Трехуровневая система переопределения секретов с безопасным хранением.
```

**Уровни:**

1. **Global** (глобальный)
   - Секреты доступны всем workspace
   - Настройка по умолчанию

2. **Workspace** (рабочая область)
   - Переопределяет глобальные секреты
   - Специфичны для конкретного проекта

3. **Server** (сервер)
   - Переопределяет workspace секреты
   - Специфичны для конкретного сервера

**Безопасность:**
- 🔒 Хранение в OS Keychain:
  - Windows: Windows Credential Store
  - macOS: Keychain
  - Linux: GNOME Keyring / KDE Wallet
- 🔒 Секреты никогда не хранятся в plain text
- 🔒 Передаются только через environment variables

**Использование:**
- В HTTP Tools: `{{SECRET_API_KEY}}`
- В Function Tools: `process.env.SECRET_API_KEY`
- Автоматическая инъекция в серверы

---

#### 6.4 AI Agent Integration

**Заголовок:** `Интеграция AI агента`

**Описание:**
```
OpenAI-совместимый API прокси для использования AI в MCP серверах.
Ключи не передаются серверам напрямую — безопасность превыше всего.
```

**Как это работает:**

1. **Настройка AI Agent**
   - Base URL (например, `https://api.openai.com/v1`)
   - API Key
   - Default Model

2. **Использование в серверах**
   - MCP Manager инжектит `MCP_AI_PROXY_URL` и `MCP_AI_PROXY_TOKEN`
   - Сервер использует прокси вместо прямого API
   - Ключи никогда не передаются серверам

3. **Контроль доступа**
   - Permissions для каждого сервера отдельно
   - Allowed Models (ограничение моделей)
   - Rate Limiting (ограничение запросов)

**Статистика:**
- 📊 Общее количество запросов и токенов
- 📊 Разбивка по серверам
- 📊 История запросов
- 📊 Прозрачность использования

**Ссылки:**
- API Reference: [AI Proxy API](https://github.com/unbywyd/tscodex-mcp-manager-app#ai-assistant)

---

#### 6.5 Permissions System

**Заголовок:** `Система разрешений`

**Описание:**
```
Очень гибкая структура разрешений для контроля доступа серверов к данным.
```

**Категории разрешений:**

1. **Environment Permissions**
   - `PATH`, `PATHEXT` — системные пути
   - `HOME`, `USERPROFILE` — домашняя директория
   - `LANG`, `LANGUAGE` — языковые настройки
   - `TEMP`, `TMP` — временные директории
   - `NODE_*`, `npm_*` — Node.js окружение
   - Custom allowlist — специфичные переменные

2. **Context Permissions**
   - `MCP_PROJECT_ROOT` — путь к проекту
   - `MCP_WORKSPACE_ID` — идентификатор workspace
   - `MCP_AUTH_TOKEN` — токен авторизации (если есть)

3. **Secrets Permissions**
   - **None** — секреты не передаются
   - **Allowlist** — только указанные секреты
   - **All** — все секреты из хранилища

4. **AI Access Permissions**
   - Разрешение на использование AI Agent
   - Allowed Models (список разрешенных моделей)
   - Rate Limit (ограничение запросов в минуту)

**Глобальные vs Workspace разрешения:**
- Глобальные разрешения применяются везде
- Workspace разрешения переопределяют глобальные
- Максимальная гибкость и безопасность

---

#### 6.6 Cursor Bridge Extension

**Заголовок:** `Cursor Bridge Extension`

**Описание:**
```
VS Code/Cursor расширение для автоматической интеграции с MCP Manager.
```

**Возможности:**

1. **Автоматическая регистрация workspace**
   - Регистрация по пути проекта
   - Автоматическая синхронизация при открытии проекта

2. **Синхронизация с MCP Manager**
   - Синхронизация состояния серверов
   - Автоматический запуск при открытии workspace

3. **Регистрация в mcp.json**
   - Автоматическая регистрация прокси MCP серверов
   - Обновление при изменении конфигурации

4. **Project Root передача**
   - Автоматическая передача PROJECT_ROOT
   - Синхронизация при изменении workspace

**Ссылки:**
- GitHub: [`tscodex-mcp-manager`](https://github.com/unbywyd/tscodex-mcp-manager)
- NPM: [@tscodex/mcp-manager](https://www.npmjs.com/package/@tscodex/mcp-manager) (если есть)

---

### 7. Секция: Security (Безопасность)

#### Заголовок:
```
Безопасность
```

#### Принципы:

1. **Secrets в OS Keychain**
   - Никогда не хранятся в plain text
   - Используется нативная безопасность ОС
   - Windows Credential Store / macOS Keychain / Linux Keyring

2. **HTTP API только localhost**
   - API доступен только на `127.0.0.1`
   - Недоступен извне
   - Защита от внешних атак

3. **AI ключи проксируются**
   - Ключи не передаются серверам напрямую
   - Используется прокси с токенами
   - Полный контроль доступа

4. **Granular permissions**
   - Разрешения для каждого сервера отдельно
   - Контроль доступа к environment, context, secrets, AI
   - Минимальные необходимые права

5. **Secure IPC**
   - Безопасное взаимодействие между UI и host
   - Защита от межпроцессных атак
   - Валидация всех запросов

#### Визуализация безопасности:

```
┌─────────────────────────────────────────┐
│         Security Layers                 │
├─────────────────────────────────────────┤
│ 1. OS Keychain (Secrets)                │
│ 2. Localhost-only API                   │
│ 3. AI Proxy (No Key Exposure)           │
│ 4. Granular Permissions                 │
│ 5. Secure IPC                           │
└─────────────────────────────────────────┘
```

---

### 8. Секция: SDK & Packages

#### Заголовок:
```
SDK & Packages
```

#### Описание:
```
Пакеты для создания MCP серверов и интеграции с системой.
```

#### Таблица пакетов:

| Package | Description | Links |
|---------|-------------|-------|
| **@tscodex/mcp-sdk** | Core SDK для создания MCP серверов. База для быстрого создания HTTP MCP серверов. | [npm](https://www.npmjs.com/package/@tscodex/mcp-sdk) / [GitHub](https://github.com/unbywyd/tscodex-mcp-sdk) |
| **@tscodex/mcp-images** | Image processing, stock search, AI generation. Пример сервера на базе SDK. | [npm](https://www.npmjs.com/package/@tscodex/mcp-images) / [GitHub](https://github.com/unbywyd/tscodex-mcp-images) |
| **@tscodex/mcp-server-example** | Example MCP server implementation. Примеры и best practices. | [npm](https://www.npmjs.com/package/@tscodex/mcp-server-example) / [GitHub](https://github.com/unbywyd/tscodex-mcp-server-example) |

#### Особенности SDK:

- ✅ Быстрое создание HTTP MCP серверов
- ✅ Поддержка workspace context
- ✅ Автоматическая обработка заголовков
- ✅ Интеграция с MCP Manager
- ✅ TypeScript поддержка

#### Пример использования:

```typescript
import { createMCPServer } from '@tscodex/mcp-sdk';

const server = createMCPServer({
  name: 'my-server',
  tools: [
    {
      name: 'my_tool',
      description: 'My custom tool',
      handler: async (params) => {
        // Access workspace context
        const projectRoot = process.env.MCP_PROJECT_ROOT;
        return { result: 'success' };
      }
    }
  ]
});

server.start();
```

---

### 9. Секция: Installation

#### Заголовок:
```
Установка
```

#### Windows:

```markdown
### Windows

1. Перейдите на [страницу релизов](https://github.com/unbywyd/tscodex-mcp-manager-app/releases)
2. Скачайте последнюю версию `.exe` установщика
3. Запустите установщик и следуйте инструкциям
4. MCP Manager автоматически добавится в меню Пуск
```

#### macOS:

```markdown
### macOS

**Важно:** Приложение создано для Windows. macOS версия требует code signing.

**Для разработчиков:**
- Вы можете самостоятельно собрать и подписать приложение из исходников
- Ресурсы доступны в [репозитории](https://github.com/unbywyd/tscodex-mcp-manager-app)
- Для сотрудничества — свяжитесь со мной

**Сборка из исходников:**
```bash
git clone https://github.com/unbywyd/tscodex-mcp-manager-app.git
cd tscodex-mcp-manager-app
npm install
npm run build:mac
```
```

#### Linux:

```markdown
### Linux

1. Перейдите на [страницу релизов](https://github.com/unbywyd/tscodex-mcp-manager-app/releases)
2. Скачайте `.AppImage` или `.deb` пакет
3. Для AppImage: `chmod +x MCP-Manager.AppImage && ./MCP-Manager.AppImage`
4. Для deb: `sudo dpkg -i mcp-manager.deb`
```

---

### 10. Секция: macOS Notice (выделенный блок)

#### Заголовок:
```
macOS Users
```

#### Стиль:
- Выделенный блок с предупреждением
- Желтый/оранжевый акцент
- Иконка предупреждения

#### Текст:

```
⚠️ macOS Users: Приложение создано для Windows.

macOS версия требует code signing для распространения.
Разработчики могут самостоятельно собрать и подписать приложение из исходников.

**Ресурсы:**
- Исходный код: [GitHub Repository](https://github.com/unbywyd/tscodex-mcp-manager-app)
- Инструкции по сборке: [Development Guide](https://github.com/unbywyd/tscodex-mcp-manager-app#development)

**Сотрудничество:**
Если вы хотите помочь с macOS версией или у вас есть возможность подписать приложение,
пожалуйста, свяжитесь со мной через GitHub Issues или email.
```

---

### 11. Footer (Подвал)

#### Элементы:

1. **Лицензия**
   - CC BY-NC 4.0 (non-commercial free)
   - Ссылка на лицензию

2. **Ссылки на репозитории**
   - MCP Manager App
   - Cursor Bridge Extension
   - SDK
   - MCP Images Server

3. **Контакт**
   - Для commercial licensing
   - GitHub Issues
   - Email (если есть)

4. **Копирайт**
   - © TSCodex 2024

#### Структура:

```
┌─────────────────────────────────────────────────┐
│  TSCodex                                        │
│  ─────────────────────────────────────────────  │
│                                                 │
│  Products                                       │
│  • MCP Manager                                  │
│  • Cursor Bridge                                │
│  • SDK                                          │
│                                                 │
│  Links                                          │
│  • GitHub                                       │
│  • NPM                                          │
│                                                 │
│  License: CC BY-NC 4.0                          │
│  For commercial licensing, contact us           │
│                                                 │
│  © TSCodex 2024                                 │
└─────────────────────────────────────────────────┘
```

---

## Routing

### Правила маршрутизации:

- `tscodex.com` → главная страница (лендинг)
- `tscodex.com/mcp-manager` → редирект на главную (`/`)
- Все остальные маршруты → 404 или редирект на главную

### Реализация:

**SPA (Single Page Application) - React Router:**

```javascript
// App.jsx или App.tsx
import { Routes, Route, Navigate } from 'react-router-dom';

function App() {
  return (
    <Routes>
      <Route path="/" element={<LandingPage />} />
      <Route path="/mcp-manager" element={<Navigate to="/" replace />} />
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
  );
}
```

**Или через конфигурацию роутера:**

```javascript
// router.jsx
import { createBrowserRouter, Navigate } from 'react-router-dom';
import LandingPage from './pages/LandingPage';

export const router = createBrowserRouter([
  {
    path: '/',
    element: <LandingPage />,
  },
  {
    path: '/mcp-manager',
    element: <Navigate to="/" replace />,
  },
  {
    path: '*',
    element: <Navigate to="/" replace />,
  },
]);
```

**Важно:** Это обычный SPA, без SSR. Все маршруты обрабатываются на клиенте.

---

## Технические требования

### Технологии:

**⚠️ ВАЖНО: Обычный SPA, никакого SSR!**

- **Framework**: React 18+ с Vite
- **UI Components**: shadcn/ui
- **Styling**: Tailwind CSS
- **Routing**: React Router v6+
- **Animations**: Framer Motion
- **Charts/Diagrams**: React Flow (для архитектурной диаграммы)
- **Icons**: Lucide React (используется в shadcn/ui)

### Структура проекта:

```
landing/
├── src/
│   ├── components/
│   │   ├── ui/              # shadcn/ui компоненты
│   │   ├── Header.tsx
│   │   ├── Hero.tsx
│   │   ├── Features.tsx
│   │   └── ...
│   ├── pages/
│   │   └── LandingPage.tsx
│   ├── lib/
│   │   └── utils.ts         # shadcn/ui utils
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css            # Tailwind CSS
├── public/
├── tailwind.config.js
├── vite.config.ts
└── package.json
```

### Настройка shadcn/ui:

```bash
# Инициализация shadcn/ui
npx shadcn-ui@latest init

# При инициализации выбрать:
# - Style: Default
# - Base color: Slate
# - CSS variables: Yes (для кастомных цветов)
```

**Важно:** После инициализации обновить `src/lib/utils.ts` для поддержки темной темы:

```tsx
// src/lib/utils.ts
import { type ClassValue, clsx } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

**Добавление компонентов:**
```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add card
npx shadcn-ui@latest add badge
npx shadcn-ui@latest add separator
npx shadcn-ui@latest add scroll-area
# и другие необходимые компоненты
```

**Настройка темной темы в globals.css:**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 158 64% 52%; /* #00d4aa */
    --primary-foreground: 222.2 84% 4.9%;
    --secondary: 217.2 91.2% 59.8%; /* #0ea5e9 */
    --secondary-foreground: 222.2 84% 4.9%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 158 64% 52%;
    --radius: 0.5rem;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

### Tailwind CSS конфигурация:

```javascript
// tailwind.config.js
module.exports = {
  darkMode: ['class'],
  content: [
    './src/**/*.{js,jsx,ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        background: '#0a0a0f',
        surface: '#12121a',
        border: '#1e1e2e',
        primary: {
          DEFAULT: '#00d4aa',
          dark: '#00b894',
        },
        secondary: '#0ea5e9',
        text: {
          primary: '#ffffff',
          secondary: '#9ca3af',
        },
      },
      backgroundImage: {
        'gradient-accent': 'linear-gradient(135deg, #00d4aa, #0ea5e9)',
        'gradient-bg': 'linear-gradient(180deg, #0a0a0f 0%, #0f1419 100%)',
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
}
```

### Производительность:

- Lazy loading для секций (React.lazy)
- Оптимизация изображений (WebP, AVIF)
- Code splitting через Vite
- SEO оптимизация через react-helmet-async или аналоги

### Адаптивность:

- Mobile-first подход
- Breakpoints: mobile (320px+), tablet (768px+), desktop (1024px+)
- Адаптивная типографика через Tailwind CSS

### Деплой:

- **Static Hosting**: Vercel, Netlify, Cloudflare Pages
- **Build**: `npm run build` создает статические файлы в `dist/`
- **Routing**: Настроить redirects для `/mcp-manager` → `/` на уровне хостинга

**Примеры конфигурации redirects:**

**Vercel (`vercel.json`):**
```json
{
  "rewrites": [
    { "source": "/mcp-manager", "destination": "/" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

**Netlify (`netlify.toml` или `_redirects`):**
```
/mcp-manager    /   200
/*    /index.html   200
```

**Cloudflare Pages (`_redirects` в `public/`):**
```
/mcp-manager    /   200
/*    /index.html   200
```

### package.json пример:

```json
{
  "name": "tscodex-landing",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "react-helmet-async": "^2.0.4",
    "framer-motion": "^10.16.16",
    "reactflow": "^11.10.1",
    "lucide-react": "^0.294.0",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "eslint": "^8.55.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6",
    "tailwindcss-animate": "^1.0.7",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
```

---

## Контент для секций

### Hero Section

**Заголовок:**
```
TSCodex - LLM Development Tools
```

**Подзаголовок:**
```
Инструменты для работы с Large Language Models
```

**Описание:**
```
MCP Manager — standalone desktop приложение для управления MCP серверами.
Централизованный хаб для настройки, запуска и мониторинга MCP серверов
с гранулярным контролем безопасности и интеграцией AI.
```

**CTA:**
- Download (ведет на GitHub Releases)
- View on GitHub (ведет на репозиторий)

---

### Что такое MCP Manager

**Текст:**
```
MCP Manager — это standalone desktop приложение на Electron для управления
MCP (Model Context Protocol) серверами. Оно решает ключевую проблему работы
с MCP серверами в реальных проектах: каждый проект в Cursor требует свой
workspace, и один сервер должен работать с разными рабочими областями.

MCP Manager создает workspace прокси для каждого проекта, позволяя одному
серверу работать с множеством workspace. SDK получает заголовки от текущего
workspace и позволяет использовать один сервер с разными рабочими областями.

Cursor Bridge Extension автоматически регистрирует workspace в приложении
по пути проекта и синхронизирует курсор с нашим менеджером, также регистрируя
прокси MCP серверы в локальном mcp.json. Это обеспечивает идеальную инкапсуляцию
и связь между workspace.
```

---

### Особенности MCP Manager

**Визуальный интерфейс:**
- Отображение tools, resources, prompts каждого сервера
- JSON Schema конфигурация через UI
- Интуитивное управление

**Управление серверами:**
- Добавление через npm, npx, pnpx, yarn, bunx, local
- Запуск, остановка, перезапуск
- Мониторинг состояния в реальном времени

**Безопасность:**
- Секреты в OS Keychain
- HTTP API только localhost
- AI ключи проксируются
- Granular permissions

**AI интеграция:**
- OpenAI-совместимый API прокси
- Статистика токенов
- Rate limiting

---

## Визуальные элементы

### Графики и диаграммы:

1. **Архитектурная диаграмма** (React Flow)
   - Интерактивная диаграмма взаимодействия компонентов
   - Анимация при скролле
   - Кликабельные элементы с описаниями

2. **Workspace Proxying схема**
   - Визуализация одного сервера → множество workspace
   - Показ передачи PROJECT_ROOT

3. **Security Layers**
   - Визуализация уровней безопасности
   - Иконки для каждого уровня

4. **Features карточки**
   - Иконки для каждой функции
   - Градиентные акценты
   - Hover эффекты

---

## SEO оптимизация

### Для SPA используется react-helmet-async:

**Установка:**
```bash
npm install react-helmet-async
```

**Настройка в App.tsx:**
```tsx
import { HelmetProvider } from 'react-helmet-async';

function App() {
  return (
    <HelmetProvider>
      <Routes>
        {/* ... */}
      </Routes>
    </HelmetProvider>
  );
}
```

**Использование в компонентах:**
```tsx
import { Helmet } from 'react-helmet-async';

function LandingPage() {
  return (
    <>
      <Helmet>
        <title>TSCodex - LLM Development Tools | MCP Manager</title>
        <meta 
          name="description" 
          content="MCP Manager - standalone desktop приложение для управления MCP серверами. Инструменты для работы с Large Language Models." 
        />
        <meta 
          name="keywords" 
          content="MCP, Model Context Protocol, LLM, AI, Cursor, VS Code, MCP Manager" 
        />
        
        {/* Open Graph */}
        <meta property="og:title" content="TSCodex - LLM Development Tools" />
        <meta 
          property="og:description" 
          content="MCP Manager - standalone desktop приложение для управления MCP серверами" 
        />
        <meta property="og:image" content="/og-image.png" />
        <meta property="og:url" content="https://tscodex.com" />
        <meta property="og:type" content="website" />
        
        {/* Twitter Card */}
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content="TSCodex - LLM Development Tools" />
        <meta name="twitter:description" content="MCP Manager - standalone desktop приложение" />
        <meta name="twitter:image" content="/og-image.png" />
      </Helmet>
      
      {/* Контент страницы */}
    </>
  );
}
```

### index.html (базовые мета-теги):

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#0a0a0f" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>TSCodex - LLM Development Tools</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>
```

### Важно для SPA:

- **Prerendering**: Рассмотреть использование prerender-spa-plugin или аналоги для статической генерации HTML
- **Sitemap**: Создать `sitemap.xml` в `public/`
- **robots.txt**: Создать `robots.txt` в `public/`
- **Structured Data**: Добавить JSON-LD для лучшего SEO

---

## Ссылки на репозитории

### GitHub:

- **MCP Manager App**: `https://github.com/unbywyd/tscodex-mcp-manager-app`
- **Cursor Bridge**: `https://github.com/unbywyd/tscodex-mcp-manager`
- **MCP Images Server**: `https://github.com/unbywyd/tscodex-mcp-images`
- **MCP SDK**: `https://github.com/unbywyd/tscodex-mcp-sdk` (если есть)

### NPM:

- **@tscodex/mcp-sdk**: `https://www.npmjs.com/package/@tscodex/mcp-sdk`
- **@tscodex/mcp-images**: `https://www.npmjs.com/package/@tscodex/mcp-images`
- **@tscodex/mcp-server-example**: `https://www.npmjs.com/package/@tscodex/mcp-server-example`

---

## Checklist реализации

### Дизайн:
- [ ] Создать дизайн-макет в Figma/Sketch
- [ ] Определить типографику
- [ ] Подготовить иконки и иллюстрации
- [ ] Создать цветовую палитру

### Разработка:
- [ ] Настроить проект (React + Vite)
- [ ] Настроить Tailwind CSS
- [ ] Инициализировать shadcn/ui
- [ ] Установить необходимые shadcn/ui компоненты
- [ ] Настроить React Router
- [ ] Реализовать Header с навигацией
- [ ] Создать Hero Section
- [ ] Реализовать секции с контентом
- [ ] Добавить React Flow диаграмму
- [ ] Создать карточки Features
- [ ] Реализовать Footer
- [ ] Настроить routing (`/mcp-manager` → `/`)

### Контент:
- [ ] Написать тексты для всех секций
- [ ] Подготовить изображения (оптимизация)
- [ ] Добавить ссылки на репозитории
- [ ] Создать SEO мета-теги

### Тестирование:
- [ ] Проверить адаптивность (mobile, tablet, desktop)
- [ ] Протестировать все ссылки
- [ ] Проверить производительность
- [ ] SEO аудит

### Деплой:
- [ ] Настроить домен tscodex.com
- [ ] Настроить SSL
- [ ] Деплой на хостинг
- [ ] Проверить доступность

---

## Дополнительные заметки

### Тон и стиль:

- **Профессиональный**, но доступный
- **Технический**, но понятный
- **Современный** AI-стиль
- **Минималистичный** дизайн

### Приоритеты:

1. **Ясность** — понятное объяснение концепции
2. **Визуализация** — графики и диаграммы
3. **Ссылки** — все ссылки на GitHub и NPM
4. **Безопасность** — акцент на безопасности
5. **macOS** — заметка о macOS версии

---

## Контакты для сотрудничества

Если кто-то хочет помочь с macOS версией или другими аспектами проекта:

- **GitHub Issues**: [Создать Issue](https://github.com/unbywyd/tscodex-mcp-manager-app/issues)
- **Email**: (указать если есть)

---

## Лицензия

**CC BY-NC 4.0** (Creative Commons Attribution-NonCommercial 4.0)

- ✅ Бесплатно для некоммерческого использования
- ✅ Требуется указание авторства
- ❌ Коммерческое использование требует отдельной лицензии

Для коммерческого лицензирования — свяжитесь с автором.

---

## Версия документа

**Версия:** 1.0  
**Дата:** 2024  
**Автор:** TSCodex

---

## Заключение

Этот документ описывает полный план реализации лендинг пейджа для tscodex.com. Все секции детально описаны с учетом оригинального посыла и требований. Документ может использоваться как техническое задание для дизайнера и разработчика.

**Ключевые моменты:**
- Темные тона с бирюзовым оттенком (AI стиль)
- Подробное описание всех функций
- Визуализация архитектуры
- Акцент на безопасности
- macOS заметка
- Все ссылки на GitHub и NPM
- Routing: `/mcp-manager` → `/`

